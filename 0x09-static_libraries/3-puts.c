#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string with a newline
 * @str: the string
 * Return: the string
 */
void _puts(char *str)

{
_puts("\"Programming is like building a multilingual puzzle");
return;
}
