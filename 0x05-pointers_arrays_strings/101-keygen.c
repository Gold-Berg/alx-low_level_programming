#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - generates random passwords
 * Return: Always 0 (success)
 */

int main(void)
{
	char password[64];
	int in = 0, sum = 0, diff1, diff2;

	strand(time(0));
	while (sum < 2772)
	{
		password[index] = 33 + rand() % 94;
		sum += password[index++]

